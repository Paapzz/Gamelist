name: Update HLTB (TEST MODE - 9 games)

on:
  # ТЕСТОВЫЙ РЕЖИМ - только ручной запуск
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # Чанк 0: игры 1-3 (ТЕСТОВЫЙ РЕЖИМ)
  run-hltb-chunk-0:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    concurrency:
      group: hltb-worker-${{ github.run_id }}-chunk-0
      cancel-in-progress: false

    env:
      PYTHONUNBUFFERED: 1
      HLTB_DEBUG: "true"
      CHUNK_INDEX: "0"
      CHUNK_SIZE: "3"

    steps:
      - name: Checkout (with token for push)
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-hltb-worker
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install required Python packages
        run: |
          python -m pip install --upgrade pip
          python -m pip install playwright pyee requests

      - name: Install Playwright browsers
        run: |
          python -m playwright install chromium

      - name: Show basic versions (non-fatal)
        run: |
          python -V
          python - <<'PY'
          import importlib
          for pkg in ("playwright","pyee","requests"):
              try:
                  mod = importlib.import_module(pkg)
                  print(pkg, getattr(mod, "__version__", "ok"))
              except Exception as e:
                  print(pkg, "NOT INSTALLED:", e)
          PY

      - name: "Run HLTB worker (Chunk 0: games 1-3) - TEST MODE"
        env:
          HLTB_DEBUG: ${{ env.HLTB_DEBUG }}
          CHUNK_INDEX: ${{ env.CHUNK_INDEX }}
          CHUNK_SIZE: ${{ env.CHUNK_SIZE }}
        run: |
          echo "Starting hltb_worker.py (DEBUG=${HLTB_DEBUG}, CHUNK_INDEX=${CHUNK_INDEX}, CHUNK_SIZE=${CHUNK_SIZE}) - TEST MODE"
          python hltb_worker.py

      - name: Upload debug dumps (if any)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hltb-debug-dumps-chunk-0
          path: |
            debug_*.json
            hltb_debug.log
            progress.json
          if-no-files-found: warn

      - name: Upload hltb data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hltb-data-chunk-0
          path: |
            hltb_data/*.json
          if-no-files-found: warn

      - name: Commit & push results (Chunk 0)
        if: always()
        env:
          GIT_AUTHOR_NAME: "HLTB Worker"
          GIT_AUTHOR_EMAIL: "hltb-worker@users.noreply.github.com"
        run: |
          set -euo pipefail
          echo "Preparing to commit results for chunk 0..."
          git config user.name "${GIT_AUTHOR_NAME}"
          git config user.email "${GIT_AUTHOR_EMAIL}"
          git checkout main

          CHANGED=0
          if ls hltb_data/*.json 1> /dev/null 2>&1; then
            git add hltb_data/*.json
            CHANGED=1
          fi
          if [ -f index111.html ]; then
            git add index111.html
            CHANGED=1
          fi
          if [ -f progress.json ]; then
            git add progress.json
            CHANGED=1
          fi

          if [ "$CHANGED" -eq 0 ]; then
            echo "No output files to commit. Exiting."
            exit 0
          fi

          COMMIT_MSG="HLTB worker: chunk 0 (games 1-3) TEST MODE $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          
          if git diff --cached --quiet; then
            echo "No changes staged for commit"
          else
            git commit -m "${COMMIT_MSG}"
          fi

          git fetch origin main

          set +e
          git merge --no-edit -s recursive -X ours origin/main
          MERGE_RC=$?
          set -e

          if [ $MERGE_RC -ne 0 ]; then
            echo "⚠️ Merge returned non-zero rc=${MERGE_RC}. Will create fallback branch to avoid overwriting remote unexpectedly."
            BRANCH="hltb-worker-fallback-$(date -u +%Y%m%d%H%M%S)"
            git checkout -b "${BRANCH}"
            git push origin "${BRANCH}"
            echo "🔀 Pushed fallback branch: ${BRANCH}. Please review and merge manually."
            exit 0
          fi

          if ls hltb_data/*.json 1> /dev/null 2>&1; then
            git add hltb_data/*.json
          fi
          if [ -f index111.html ]; then
            git add index111.html
          fi
          if [ -f progress.json ]; then
            git add progress.json
          fi

          if git diff --cached --quiet; then
            echo "No post-merge changes to commit"
          else
            git commit -m "${COMMIT_MSG} (post-merge)"
          fi

          set +e
          git push origin HEAD:main
          PUSH_RC=$?
          set -e

          if [ $PUSH_RC -eq 0 ]; then
            echo "✅ Pushed results to main"
            exit 0
          else
            echo "⚠️ Push to main failed (RC=$PUSH_RC). Creating fallback branch and pushing there."
            BRANCH="hltb-worker-fallback-$(date -u +%Y%m%d%H%M%S)"
            git checkout -b "${BRANCH}"
            git push origin "${BRANCH}"
            echo "🔀 Pushed fallback branch: ${BRANCH}. Please merge manually."
            exit 0
          fi

  # Чанк 1: игры 4-6 (ТЕСТОВЫЙ РЕЖИМ)
  run-hltb-chunk-1:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    concurrency:
      group: hltb-worker-${{ github.run_id }}-chunk-1
      cancel-in-progress: false
    needs: run-hltb-chunk-0

    env:
      PYTHONUNBUFFERED: 1
      HLTB_DEBUG: "true"
      CHUNK_INDEX: "1"
      CHUNK_SIZE: "3"

    steps:
      - name: Checkout (with token for push)
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Pull latest changes from previous chunk
        run: |
          git config user.name "HLTB Worker"
          git config user.email "hltb-worker@users.noreply.github.com"
          git pull origin main

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-hltb-worker
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install required Python packages
        run: |
          python -m pip install --upgrade pip
          python -m pip install playwright pyee requests

      - name: Install Playwright browsers
        run: |
          python -m playwright install chromium

      - name: Show basic versions (non-fatal)
        run: |
          python -V
          python - <<'PY'
          import importlib
          for pkg in ("playwright","pyee","requests"):
              try:
                  mod = importlib.import_module(pkg)
                  print(pkg, getattr(mod, "__version__", "ok"))
              except Exception as e:
                  print(pkg, "NOT INSTALLED:", e)
          PY

      - name: "Run HLTB worker (Chunk 1: games 4-6) - TEST MODE"
        env:
          HLTB_DEBUG: ${{ env.HLTB_DEBUG }}
          CHUNK_INDEX: ${{ env.CHUNK_INDEX }}
          CHUNK_SIZE: ${{ env.CHUNK_SIZE }}
        run: |
          echo "Starting hltb_worker.py (DEBUG=${HLTB_DEBUG}, CHUNK_INDEX=${CHUNK_INDEX}, CHUNK_SIZE=${CHUNK_SIZE}) - TEST MODE"
          python hltb_worker.py

      - name: Upload debug dumps (if any)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hltb-debug-dumps-chunk-1
          path: |
            debug_*.json
            hltb_debug.log
            progress.json
          if-no-files-found: warn

      - name: Upload hltb data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hltb-data-chunk-1
          path: |
            hltb_data/*.json
          if-no-files-found: warn

      - name: Commit & push results (Chunk 1)
        if: always()
        env:
          GIT_AUTHOR_NAME: "HLTB Worker"
          GIT_AUTHOR_EMAIL: "hltb-worker@users.noreply.github.com"
        run: |
          set -euo pipefail
          echo "Preparing to commit results for chunk 1..."
          git config user.name "${GIT_AUTHOR_NAME}"
          git config user.email "${GIT_AUTHOR_EMAIL}"
          git checkout main

          CHANGED=0
          if ls hltb_data/*.json 1> /dev/null 2>&1; then
            git add hltb_data/*.json
            CHANGED=1
          fi
          if [ -f index111.html ]; then
            git add index111.html
            CHANGED=1
          fi
          if [ -f progress.json ]; then
            git add progress.json
            CHANGED=1
          fi

          if [ "$CHANGED" -eq 0 ]; then
            echo "No output files to commit. Exiting."
            exit 0
          fi

          COMMIT_MSG="HLTB worker: chunk 1 (games 4-6) TEST MODE $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          
          if git diff --cached --quiet; then
            echo "No changes staged for commit"
          else
            git commit -m "${COMMIT_MSG}"
          fi

          git fetch origin main

          set +e
          git merge --no-edit -s recursive -X ours origin/main
          MERGE_RC=$?
          set -e

          if [ $MERGE_RC -ne 0 ]; then
            echo "⚠️ Merge returned non-zero rc=${MERGE_RC}. Will create fallback branch to avoid overwriting remote unexpectedly."
            BRANCH="hltb-worker-fallback-$(date -u +%Y%m%d%H%M%S)"
            git checkout -b "${BRANCH}"
            git push origin "${BRANCH}"
            echo "🔀 Pushed fallback branch: ${BRANCH}. Please review and merge manually."
            exit 0
          fi

          if ls hltb_data/*.json 1> /dev/null 2>&1; then
            git add hltb_data/*.json
          fi
          if [ -f index111.html ]; then
            git add index111.html
          fi
          if [ -f progress.json ]; then
            git add progress.json
          fi

          if git diff --cached --quiet; then
            echo "No post-merge changes to commit"
          else
            git commit -m "${COMMIT_MSG} (post-merge)"
          fi

          set +e
          git push origin HEAD:main
          PUSH_RC=$?
          set -e

          if [ $PUSH_RC -eq 0 ]; then
            echo "✅ Pushed results to main"
            exit 0
          else
            echo "⚠️ Push to main failed (RC=$PUSH_RC). Creating fallback branch and pushing there."
            BRANCH="hltb-worker-fallback-$(date -u +%Y%m%d%H%M%S)"
            git checkout -b "${BRANCH}"
            git push origin "${BRANCH}"
            echo "🔀 Pushed fallback branch: ${BRANCH}. Please merge manually."
            exit 0
          fi

  # Чанк 2: игры 7-9 (ТЕСТОВЫЙ РЕЖИМ)
  run-hltb-chunk-2:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    concurrency:
      group: hltb-worker-${{ github.run_id }}-chunk-2
      cancel-in-progress: false
    needs: run-hltb-chunk-1

    env:
      PYTHONUNBUFFERED: 1
      HLTB_DEBUG: "true"
      CHUNK_INDEX: "2"
      CHUNK_SIZE: "3"

    steps:
      - name: Checkout (with token for push)
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Pull latest changes from previous chunk
        run: |
          git config user.name "HLTB Worker"
          git config user.email "hltb-worker@users.noreply.github.com"
          git pull origin main

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-hltb-worker
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install required Python packages
        run: |
          python -m pip install --upgrade pip
          python -m pip install playwright pyee requests

      - name: Install Playwright browsers
        run: |
          python -m playwright install chromium

      - name: Show basic versions (non-fatal)
        run: |
          python -V
          python - <<'PY'
          import importlib
          for pkg in ("playwright","pyee","requests"):
              try:
                  mod = importlib.import_module(pkg)
                  print(pkg, getattr(mod, "__version__", "ok"))
              except Exception as e:
                  print(pkg, "NOT INSTALLED:", e)
          PY

      - name: "Run HLTB worker (Chunk 2: games 7-9) - TEST MODE"
        env:
          HLTB_DEBUG: ${{ env.HLTB_DEBUG }}
          CHUNK_INDEX: ${{ env.CHUNK_INDEX }}
          CHUNK_SIZE: ${{ env.CHUNK_SIZE }}
        run: |
          echo "Starting hltb_worker.py (DEBUG=${HLTB_DEBUG}, CHUNK_INDEX=${CHUNK_INDEX}, CHUNK_SIZE=${CHUNK_SIZE}) - TEST MODE"
          python hltb_worker.py

      - name: Upload debug dumps (if any)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hltb-debug-dumps-chunk-2
          path: |
            debug_*.json
            hltb_debug.log
            progress.json
          if-no-files-found: warn

      - name: Upload hltb data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hltb-data-chunk-2
          path: |
            hltb_data/*.json
          if-no-files-found: warn

      - name: Commit & push results (Chunk 2)
        if: always()
        env:
          GIT_AUTHOR_NAME: "HLTB Worker"
          GIT_AUTHOR_EMAIL: "hltb-worker@users.noreply.github.com"
        run: |
          set -euo pipefail
          echo "Preparing to commit results for chunk 2..."
          git config user.name "${GIT_AUTHOR_NAME}"
          git config user.email "${GIT_AUTHOR_EMAIL}"
          git checkout main

          CHANGED=0
          if ls hltb_data/*.json 1> /dev/null 2>&1; then
            git add hltb_data/*.json
            CHANGED=1
          fi
          if [ -f index111.html ]; then
            git add index111.html
            CHANGED=1
          fi
          if [ -f progress.json ]; then
            git add progress.json
            CHANGED=1
          fi

          if [ "$CHANGED" -eq 0 ]; then
            echo "No output files to commit. Exiting."
            exit 0
          fi

          COMMIT_MSG="HLTB worker: chunk 2 (games 7-9) TEST MODE $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          
          if git diff --cached --quiet; then
            echo "No changes staged for commit"
          else
            git commit -m "${COMMIT_MSG}"
          fi

          git fetch origin main

          set +e
          git merge --no-edit -s recursive -X ours origin/main
          MERGE_RC=$?
          set -e

          if [ $MERGE_RC -ne 0 ]; then
            echo "⚠️ Merge returned non-zero rc=${MERGE_RC}. Will create fallback branch to avoid overwriting remote unexpectedly."
            BRANCH="hltb-worker-fallback-$(date -u +%Y%m%d%H%M%S)"
            git checkout -b "${BRANCH}"
            git push origin "${BRANCH}"
            echo "🔀 Pushed fallback branch: ${BRANCH}. Please review and merge manually."
            exit 0
          fi

          if ls hltb_data/*.json 1> /dev/null 2>&1; then
            git add hltb_data/*.json
          fi
          if [ -f index111.html ]; then
            git add index111.html
          fi
          if [ -f progress.json ]; then
            git add progress.json
          fi

          if git diff --cached --quiet; then
            echo "No post-merge changes to commit"
          else
            git commit -m "${COMMIT_MSG} (post-merge)"
          fi

          set +e
          git push origin HEAD:main
          PUSH_RC=$?
          set -e

          if [ $PUSH_RC -eq 0 ]; then
            echo "✅ Pushed results to main"
            exit 0
          else
            echo "⚠️ Push to main failed (RC=$PUSH_RC). Creating fallback branch and pushing there."
            BRANCH="hltb-worker-fallback-$(date -u +%Y%m%d%H%M%S)"
            git checkout -b "${BRANCH}"
            git push origin "${BRANCH}"
            echo "🔀 Pushed fallback branch: ${BRANCH}. Please merge manually."
            exit 0
          fi
